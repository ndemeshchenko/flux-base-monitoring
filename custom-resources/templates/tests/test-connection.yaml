apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "custom-resources.fullname" . }}-test-connection"
  labels:
    {{- include "custom-resources.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "custom-resources.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
