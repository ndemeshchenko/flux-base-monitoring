apiVersion: v1
kind: Service
metadata:
  name: {{ include "custom-resources.fullname" . }}
  labels:
    {{- include "custom-resources.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "custom-resources.selectorLabels" . | nindent 4 }}
