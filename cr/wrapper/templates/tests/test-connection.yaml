apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wrapper.fullname" . }}-test-connection"
  labels:
    {{- include "wrapper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wrapper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
